podTemplate(
    cloud: 'kubernetes', 
    inheritFrom: 'jenkins-slave', 
    name: 'jenkins-devops', 
    namespace: 'devops'
    ) {
    // some block
    stage("代码拉取") {
        echo "123456"
    }
}